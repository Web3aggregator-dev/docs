# Aggregation Layer

The aggregation layer routes each request to the most suitable
upstream provider based on health, latency, and method support.

!!! info "Coming soon"

    A deeper walk through routing decisions, fallback behavior, and
    consistency guarantees is in preparation. See
    [Architecture](architecture.md) for a higher-level overview.
