# Authentication

All Web3aggregator API requests authenticate using a bearer token
passed in the `Authorization` header.

!!! info "Coming soon"

    Detailed coverage of authentication options, scopes, and key
    rotation is in preparation. See the
    [Quickstart](quickstart.md#1-authenticate) for the basic usage
    pattern.
