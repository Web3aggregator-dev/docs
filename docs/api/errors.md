# Errors

All Web3aggregator endpoints return errors using a consistent JSON
envelope with a stable error code, a human-readable message, and a
correlation identifier for support requests.

!!! info "Coming soon"

    The full error code reference is in preparation. Until then,
    rely on the HTTP status code and the `error.code` field in the
    response body.
